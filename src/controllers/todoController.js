const addTask = async (req, res, next) => {
    try {
        // const path = './tasks.json'
        // const task = json.stringify(req.body)

    } catch (err) {
        console.log(err)
        // next(err)
        
    }
}